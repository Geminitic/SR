import { useContext } from 'react';
import { LocationProvider } from '@/providers/LocationProvider';

// Re-export the useLocation hook from the provider for convenience
export { useLocation } from '@/providers/LocationProvider';